{
  "name": "avacom_frontend-deploy",
  "version": "1.0.0",
  "description": "Scripts para deploy do projeto avacom_frontend",
  "private": true,
  "scripts": {
    "predeploy": "cd front-end && npm install && npm run build",
    "deploy": "gh-pages -d front-end/dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}